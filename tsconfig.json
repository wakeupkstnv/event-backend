{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2016",                                  /* Устанавливает версию JavaScript для выводимого кода */
    "module": "commonjs",                                /* Устанавливает формат модулей для вывода */
    "rootDir": "./src",                                  /* Указывает корневую директорию исходных файлов */
    "outDir": "./dist",                                  /* Указывает выходную директорию для скомпилированных файлов */
    "esModuleInterop": true,                             /* Облегчает импорт CommonJS модулей */
    "forceConsistentCasingInFileNames": true,            /* Гарантирует корректность регистра в путях импортов */

    /* Type Checking */
    "strict": true,                                      /* Включает все строгие проверки типов */
    "noImplicitAny": false,                              /* Разрешает неявный тип any */
    "strictNullChecks": true,                            /* Включает строгую проверку null и undefined */
    "strictFunctionTypes": true,                         /* Включает строгую проверку типов функций */

    /* Completeness */
    "skipLibCheck": true                                 /* Пропускает проверку типов в d.ts файлах */
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
